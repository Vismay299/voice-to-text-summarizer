{
  "name": "@voice/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.19.0",
    "@types/pg": "^8.20.0",
    "@voice/shared": "file:../../packages/shared",
    "pg": "^8.20.0"
  }
}
